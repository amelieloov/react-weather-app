//Start weather in Stockholm
const WeatherItem = ({item}) => {
    return (
        <>
            <div>
                <h1>{item.name}</h1>
            </div>
        </>
    )
}

export default WeatherItem;